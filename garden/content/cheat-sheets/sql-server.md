---
title: SQL Server cheat sheet
description: Context, kiểu ngày, NULL và kiểm thử batch.
type: cheatsheet
topics: [sql-server, ssms]
related: [setup, date-conversion, null-comparison, multi-row-trigger, debugging]
provenance: original-practice
technicalSources: [TECH-MS01]
---
# SQL Server

`SELECT DB_NAME()` · ISO date/parameter · `IS NULL` · khoảng nửa mở cho datetime · set-based trigger · replay một dòng và nhiều dòng. Xem [[debugging]].
