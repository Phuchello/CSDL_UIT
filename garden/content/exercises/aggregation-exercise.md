---
title: Bài tập rèn luyện — Hạt dữ liệu gom nhóm (Aggregation Grain)
description: Xác định grain trước khi GROUP BY và HAVING.
type: exercise
topics: [group-by, having, join]
related: [lab-02, group-by-8120, unexpected-duplicates]
provenance: original-practice
---
# Bài tập rèn luyện — Hạt dữ liệu gom nhóm (Aggregation Grain)

Nêu một dòng kết quả đại diện cho gì, nối bảng theo key, rồi chọn `GROUP BY` và `HAVING`. Nếu Msg 8120 xuất hiện, kiểm tra từng cột SELECT theo [[group-by-8120]].
