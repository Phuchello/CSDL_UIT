---
title: Observed pattern — RBTV and trigger
description: Mẫu suy luận bảng tác động, trigger và batch.
type: exam-pattern
topics: [exam, integrity, trigger]
related: [rbtv-impact, multi-row-trigger, multi-row-trigger-failure]
provenance: reconstructed-exam-pattern
courseEvidence: [UIT-E04]
---
# Observed pattern — RBTV and trigger

Pattern được quan sát là lập impact table, chọn ràng buộc khai báo hay trigger, rồi phân tích `inserted`/`deleted` cho INSERT/UPDATE/DELETE. Không gọi đây là dự đoán đề; xem [[sources]].
