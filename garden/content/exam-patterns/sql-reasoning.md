---
title: Observed pattern — SQL reasoning
description: Mẫu suy luận SQL về NULL, JOIN, GROUP BY và EXISTS.
type: exam-pattern
topics: [exam, sql, null, join, exists]
related: [null-comparison, not-in-null, group-by-8120, double-not-exists]
provenance: reconstructed-exam-pattern
courseEvidence: [UIT-E02]
---
# Observed pattern — SQL reasoning

Pattern được quan sát gồm dự đoán kết quả, chỉ ra dòng bị loại bởi NULL/3VL, sửa JOIN và phân biệt WHERE với HAVING. Câu trả lời cần nêu grain và miền lượng từ, không chỉ đưa câu SQL.
