# v1.1 Quartz D2 QA

## Upstream and scope

- Pinned Quartz upstream: `v5` commit `075afd3f712da0088a07f5284a7b3aba37dd61b6`, package `5.0.0`, MIT; exact URL and Node/npm requirements are recorded in `garden/QUARTZ_UPSTREAM.md`.
- Candidate branch: `v1.1-quartz-garden`, based on frozen D1 commit `922afe07bea7f28abf30c49054159a09a31be743`.
- Frozen `book/`, `practice/`, `research/v1.1_phase_a/`, `site/`, and prior PDFs were not edited.

## Content and structure

- Notes: 57 total — theory 14, practice 8, exercise 4, exam-pattern 5, error 19, cheatsheet 4, source 3.
- Required normal routes and theory/practice slugs: PASS.
- Mandatory Graph Chain: PASS. Real in-body Markdown edges (`[[...]]`) verified bidirectionally across the complete chain: `theory/division` ↔ `theory/double-not-exists` ↔ `practice/lab-03` ↔ `errors/wrong-universal-candidate`.
- Core Notes Standalone Depth: PASS. All 11 core notes (`relational-algebra`, `division`, `double-not-exists`, `rbtv-impact`, `functional-dependencies`, `closure`, `candidate-keys`, `minimal-cover`, `3nf`, `bcnf`, `lossless-decomposition`) deepened to full standalone teaching quality with formal definitions, algorithm steps, and dry-run tables.
- Frontmatter `related` targets: PASS. All 7 previously ambiguous or broken targets resolved uniquely.
- Provenance and Evidence Wording: PASS. All synthetic `TECH-MS*` and `UIT-E*` IDs removed. Community-mirror artifacts (`EXAM-2024-2025-HK1-FINAL-01`) explicitly distinguished from official university exams (Tier A/B); zero overclaim phrases ("luôn xuất hiện", "bắt buộc phải dùng trigger" eliminated).
- Public copy safety: PASS for content (no internal build labels, hype terms, emoji, raw attachments, or unverified claims).

## Build and links

- `npm ci`: PASS (Quartz v5 lockfile, no `node_modules` committed).
- Actual command: `node quartz/bootstrap-cli.mjs build -d content -o public`: PASS; 109 emitted files in 1.0s.
- Static internal-link crawl: PASS. 2,810 internal links checked across 59 HTML files; exactly 0 broken links.
- Base path: PASS (`/CSDL_UIT/`); no root-host assumption.
- Search: PASS for `phép chia`, `phep chia`, `trigger`, `NULL`, `bao đóng`, `bcnf`, `khóa ứng viên`, and `group by`.
- Responsive/accessibility browser checks: PASS at 1440, 900, and 390 px; horizontal overflow 0; keyboard-focusable controls present; semantic headings and labelled search present.

## Fixture, Trigger, and PDF contracts

- `practice/sql/01_schema.sql` fixture contract: PASS. A04 uses `tr_students`, `tr_courses`, `tr_results`, sample `S001`, and is explicitly STATIC until execution.
- Canonical Trigger contract: PASS. `practice/multi-row-trigger.md` implements set-based event discrimination (`WHERE i.EmployeeId IS NULL` for true `DELETE`, `UPDATE(DeptId)` check for department transfer) and explicitly distinguishes schema-level `DeptId NOT NULL` DDL rejection from defensive trigger logic (`i.DeptId IS NULL`).
- Strict Single PDF Contract: PASS. Exactly one public naming convention (lowercase: `it004_csdl_uit_lythuyet_votrongphuc.pdf` and `it004_csdl_uit_thuchanh_votrongphuc.pdf`), copying only 1 file per handbook. Built output validated: exactly 2 PDFs exist in `garden/quartz/static/pdfs/` and `garden/public/static/pdfs/`, matching SHA-256 with frozen handbooks. Legacy `CamNang` removed from public navigation.

## Visual and copyright gate

- Fresh visual captures: PASS. Generated from the local Quartz build into `dist/review/v1.1_quartz/`: `home-desktop.png`, `home-mobile.png`, `theory-division.png`, `sql-double-not-exists.png`, `practice-lab03.png`, `trigger-multirow.png`, `errors.png`, `search.png`, `graph-related.png`, `sources.png`.
- Visual checks: PASS by Playwright route/title/overflow and component assertions; D1 navy/teal/ochre/paper family retained, generic footer branding removed, graph/backlinks/search present, no gradients or dashboards.
- Copyright/license: PASS. No raw UIT source PDFs were copied; Quartz MIT notice and upstream license retained; CSDL_UIT notice remains authoritative.

## Blockers

- Remaining blocker: none for D2 milestone. This branch is ready for human mentor review only; it is not published to Pages, merged, tagged, or released.
