# IT004 v1.1 Practical Handbook — Full QA

## Scope

Full Phase C2 practical handbook on branch v1.1-practice-handbook. Theory, Phase A and the C1 proof artifact were not edited.

## Build and structure

- Source: practice/index.html, compiled by scripts/build_practice.py from 15 chapter fragments.
- HTML validation: PASS (all chapter files present; browser console/page errors 0; duplicate IDs 0).
- Print audit: PASS; A4 CSS page size, Georgia body font, no horizontal overflow, no tofu glyphs, and no banned editorial phrases/emojis in practice chapters.
- PDF: dist/IT004_CSDL_UIT_ThucHanh_VoTrongPhuc.pdf; 76 pages; 890,810 bytes after normalization.
- Raster review: all 76 pages rendered with Poppler; contact sheets are in dist/review/v1.1_practice_full/. No accidental blank or clipped page observed.

## Coverage

Parts 0–12 plus appendices; Labs 01–04; setup, DDL, DML, SELECT/NULL/date, joins, aggregates, subqueries, EXISTS/NOT EXISTS, universal queries, set operators, CASE, RANK/TOP WITH TIES, RBTV/triggers, debugging and practical-exam workflow.

## Expected-result consistency

| Example ID | Dataset/script | Query/result anchor | Validation mode |
|---|---|---|---|
| B01 | 02_seed.sql + 03_queries_basic.sql | Segment A/B projection; C001–C003 | STATIC |
| B02 | 02_seed.sql + 03_queries_basic.sql | half-open date range over 2024-10-15…18 | STATIC |
| B03 | 02_seed.sql + 03_queries_basic.sql | 11 joined item rows | STATIC |
| B04 | 02_seed.sql + 03_queries_basic.sql | C005 preserved with zero orders | STATIC |
| A03 | 02_seed.sql + 04_queries_advanced.sql | P007 is unsold | STATIC |
| A04 | 02_seed.sql + 04_queries_advanced.sql | Double NOT EXISTS; empty divisor stated | STATIC |
| TOP-TIE | Lab 04 tie exercise | result may exceed n at the ORDER BY boundary | ILLUSTRATIVE |

scripts/validate_practice_static.py checks tables, key columns, seed counts, trigger names and query references: PASS.

## SQL and trigger QA

- SQL Server runtime validation: NOT AVAILABLE. sqlcmd is installed, but a trusted connection to the local MSSQLSERVER instance failed (ODBC encryption/credentials), so no runtime output is claimed.
- Static semantic validation: PASS.
- Trigger multi-row QA: PASS by source inspection; triggers use inserted/deleted sets and IF EXISTS, with whole-statement rejection semantics.
- Event discrimination A–H: PASS; cases are preserved in 06_test_cases.sql and the handbook (A/B/C/G pass; D/E/F/H reject).
- NULL/date QA: PASS; IS NULL and half-open DATETIME2 ranges are explicit.
- Universal-query QA: PASS; Double NOT EXISTS, COUNT DISTINCT, duplicate retake and empty-divisor semantics are documented.
- Debugging QA: PASS; Msg 208, 207, 547, 2627/2601, 8152/2628, 8115, 8120, 512, 245, 241/242, ambiguous/invalid alias, NULL, wrong DB, FK order and universal-logic entries include symptom/cause/check/fix/prevent.

## Metadata and structural validation

Strict pypdf parsing and Poppler pdfinfo both pass. The normalized PDF has:

- Title: IT004 – Thực hành Cơ sở dữ liệu
- Author: Võ Trọng Phúc
- Subject: IT004 – Cơ sở dữ liệu — Sổ tay thực hành
- A4 (594.96 × 841.92 pt), 76 pages, unencrypted, no JavaScript, no form fields.

## Provenance and copyright

PASS. Practical source roles remain separated: TECH-Axx for Microsoft semantics, LOC-SQL-LABxx for observed progression, PRAC-xxxx for practical relevance, and UIT official context. No raw exam scans, lecture PDFs, student SQL, or QLBANHANG.xlsx were added.

## Remaining blocker

No publishing blocker. SQL runtime is unavailable in this environment and remains explicitly marked NOT AVAILABLE; static checks and rendered-PDF checks pass. Quartz, merge, tag and release are intentionally out of scope.
