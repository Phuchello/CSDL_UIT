# CSDL_UIT v1.1 — Theory Handbook QA

## Issue

Complete the Phase B2 theory-handbook redesign on `v1.1-theory-redesign` while preserving the frozen Phase A source baseline, the public v1.0 PDF, and the separate practical source.

## Files changed

- `book/css/book.css`
- `book/chapters/ch00_intro.html` through `book/chapters/ch06_fd_normalization.html`
- `book/chapters/ch07_practical.html` (print exclusion marker only)
- `book/chapters/exam_playbook.html`, `cheat_sheet.html`, `references.html`
- `book/index.html`
- `scripts/build.py`
- `scripts/build_theory_pdf.js`
- `scripts/normalize_theory_pdf.py`
- `PROJECT_STATE.md`, `TODO.md`

Generated review artifacts:

- `dist/IT004_CSDL_UIT_LyThuyet_VoTrongPhuc.pdf`
- `dist/review/v1.1_theory/contact-sheet-01.png` through `contact-sheet-04.png`

## Exact fixes

- Replaced the legacy handbook CSS with the approved B1 typography, color, grid, callout, table, code, schema, and diagram rules. The practical chapter remains in source but is hidden from theory print with `.theory-exclude`.
- Replaced the cover with Cover A Editorial Schema: only `IT004`, `CƠ SỞ DỮ LIỆU`, and `BIÊN SOẠN: VÕ TRỌNG PHÚC` are visible, plus the permitted abstract R1→R2→R3→R4 PK/FK diagram.
- Added a 1:1 / 1:N / M:N relationship-mapping mechanism figure and translated the remaining visible editorial labels into natural Vietnamese.
- Kept the Chapter 4 recall heading with its recall sheet using a targeted adjacent-heading/page-break rule; no global font or margin reduction was used.
- Kept short and medium `<pre>` blocks together in print with targeted `break-inside` rules and `white-space: pre-wrap`/`overflow-wrap` safeguards.
- Reframed Relational Algebra Exercise 13 as a general union-compatibility example. `NHANVIEN` is explicitly identified as an auxiliary relation outside canonical QLGV; the question, explanation, and renamed union formula now agree. Added the SQL bag-semantics / `DISTINCT` distinction.

## Validation result

- `scripts/build.py`: PASS — compiled 11 source fragments into `book/index.html`.
- `scripts/build_theory_pdf.js`: PASS — no console/page errors, no duplicate IDs, no horizontal overflow, no tofu glyphs, Georgia resolved, all required RA symbols present, practical section hidden, 30+ exercise labels present.
- HTML parse (`lxml`): PASS — document root and IDs parse cleanly; no duplicate IDs.
- Rendered PDF review: PASS — 64 pages rasterized with Poppler and reviewed in four contact sheets plus targeted pages. The recall title and sheet share one page; CREATE TABLE and other short SQL blocks remain readable and un-clipped; the 1:1 / 1:N / M:N figure renders; no accidental blank page, scrollbar, code clipping, or broken Vietnamese/RA glyph was observed.
- Content smoke checks: PASS — all six theory chapters, 30 Relational Algebra exercise labels/solutions, SQL `DISTINCT`/`NULL`, PK/FK and RBTV material, closure/keys, 3NF/BCNF, lossless-join and dependency-preservation statements are present; practical-only marker text is absent from the PDF.

## Final page count

64 pages (A4).

## Metadata result

Verified with Poppler after pypdf rewrite:

- Title: `IT004 – Cơ sở dữ liệu: Cẩm nang từ nền tảng đến Exam Mastery`
- Author: `Võ Trọng Phúc`
- Subject: `IT004 – Cơ sở dữ liệu`

## PDF structural validation

PASS. The final file opens and rasterizes on all 64 pages. `pdfinfo` reports no malformed xref warning, encryption, JavaScript, or suspect objects; the normalized file has a single authoritative metadata dictionary and A4 page geometry.

## Remaining blocker

None for Phase B2 theory redesign. Practice handbook authoring, Quartz construction, merge to `main`, tagging, and release remain intentionally out of scope.
