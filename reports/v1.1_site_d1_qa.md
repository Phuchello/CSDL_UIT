# CSDL_UIT v1.1 — D1 content-integrity freeze QA

Date: 2026-09-02
Branch: `v1.1-knowledge-garden`
Scope: representative static Knowledge Garden proof only; frozen theory, practice, and Phase A sources remain outside this patch.

## Issue / files changed / exact fix

- Lab 03 fixture mismatch — `site/app.js`: replaced the non-canonical order/customer example with the validated A04 `tr_students`/`tr_courses`/`tr_results` double-`NOT EXISTS` query and marked `S001` as a STATIC expected result because it was not executed in this pass.
- Trigger fixture mismatch — `site/app.js`: replaced order/amount references with the frozen `tr_departments`/`tr_employees` contract (`HeadEmployeeId`, `DeptId`, `EmployeeId`) and documented the allowed/rejected multi-row cases without a runtime claim.
- Source URL drift — `site/app.js`: synchronized TECH-A04/A05/A06/A07, UIT-O02, and UIT-O06 links to exact URLs in `research/v1.1_phase_a/source_inventory.md`; UIT-O06 uses the ledger URL verbatim.
- Visible internal build copy — `site/index.html`, `site/app.js`: removed proof/version/process language from user-facing labels while retaining source provenance links.
- Gradient contradiction — `site/styles.css`: changed the hero background from a gradient to the approved flat paper color.
- Contract automation — `scripts/validate_site_d1.py`: hardened alias-to-table resolution, added column-contract checks and a negative self-test, and retained the schema, fixture, source-ID/URL, route, visual, search, mobile, and focus gates.
- State and review evidence — `PROJECT_STATE.md`, `reports/v1.1_site_d1_qa.md`, and `dist/review/v1.1_site/README.md` documenting the six pre-patch captures.

## Required gates

ALIAS → TABLE RESOLUTION: PASS
LAB03 COLUMN CONTRACT: PASS
TRIGGER COLUMN CONTRACT: PASS
VALIDATOR NEGATIVE SELF-TEST: PASS
LAB03 FIXTURE CONTRACT: PASS
TRIGGER FIXTURE CONTRACT: PASS
SOURCE-ID SYNC: PASS
SOURCE-URL SYNC: PASS
VISIBLE INTERNAL BUILD COPY: 0
GRADIENT CHECK: PASS
BROKEN INTERNAL ROUTES: 0
ROUTE CHECK: PASS
SEARCH: PASS
MOBILE: PASS
KEYBOARD / FOCUS: PASS
THEORY UNTOUCHED: YES
PRACTICE UNTOUCHED: YES
PHASE A UNTOUCHED: YES

## Validation result

- `scripts/validate_site_d1.py`: PASS (all gates above).
- `node --check site/app.js`: PASS.
- `git diff --check`: PASS.
- Review captures: six PNGs are present under `dist/review/v1.1_site/`; regeneration was BLOCKED by the browser URL policy for localhost, so no stale image content is represented as refreshed evidence.
- Frozen-source diff (`book/`, `practice/`, `research/v1.1_phase_a/`): CLEAN.
- Canonical PDFs were not rebuilt; the frozen practice PDF remains 71 pages and is unchanged.
- PDF metadata and structural validation: NOT APPLICABLE to this static-site-only patch; existing canonical PDF was not modified.

## Remaining blocker

Screenshot regeneration remains a contained environment blocker: the browser review surface rejects the local HTTP origin with `ERR_BLOCKED_BY_CLIENT`. D2 Quartz implementation and human review remain intentionally pending.
